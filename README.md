# commit-summary

This is a simple python script that helps generate a commit summary by calling `opencode`.

What it does
- `generate_summary.py` tries to produce a commit summary by calling `opencode` and writing a temporary plan file.

How to run
1. Run `uv run generate_summary.py` to generate a commit summary.


